/**
 * Application-wide constants
 */

/**
 * Default directory for markdown pages
 */
export const DEFAULT_PAGES_DIR = 'pages';

/**
 * Default index file name for directories
 */
export const DEFAULT_INDEX_FILE = '_index.md';

/**
 * Links file name for reference links
 */
export const LINKS_FILE = '_links.md';

/**
 * Default cache TTL in milliseconds (5 minutes)
 */
export const DEFAULT_CACHE_TTL = 5 * 60 * 1000;

/**
 * Output file for sitemap/feed entries
 */
export const SITEMAP_OUTPUT_FILE = '.svelte-kit/output/client/sitemap.json';
